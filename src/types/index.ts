export type SocialType = {
  name: string;
  url: string;
  icon: any;
}

export type CardCharacterProps = {
  name: string;
  image: any;
  slug: string;
}

export type PageTitleProps = {
  title: string;
  description: string;
}